---
phase: 06-v1-gap-closure
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - compiler/compiler_test.go
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "compiler/compiler_test.go contains zero assert.Contains(t, err.Error(), ...) calls"
    - "All 21 former string-match sites now assert via assert.ErrorIs(t, err, compiler.ErrXxx)"
    - "The identityref table-driven test uses sentinel error field, not errorMsg string field"
    - "go test ./compiler/... passes with no failures"
  artifacts:
    - path: "compiler/compiler_test.go"
      provides: "All error assertions using assert.ErrorIs against sentinel vars"
      contains: "assert.ErrorIs"
  key_links:
    - from: "compiler/compiler_test.go"
      to: "compiler.ErrXxx sentinels"
      via: "assert.ErrorIs(t, err, compiler.ErrXxx) at all 21 former string-match sites"
      pattern: "assert\\.ErrorIs"
---

<objective>
Migrate all 21 assert.Contains(t, err.Error(), ...) error-assertion call sites in compiler/compiler_test.go to assert.ErrorIs(t, err, compiler.ErrXxx) sentinel assertions. Plan 01's Validator refactor is a prerequisite: without it, the 8 Validator-path sites would fail because the Validator was emitting ErrInvalidDefault for all categories.

Purpose: Error assertions are structural (refactoring-safe) instead of fragile substring checks.
Output: compiler/compiler_test.go with zero assert.Contains(t, err.Error(), ...) calls; all error assertions use typed sentinels.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@compiler/compiler_test.go
@compiler/compiler.go
@.planning/phases/06-v1-gap-closure/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

Sentinel vars (compiler/compiler.go lines 16-27):
```go
var (
    ErrCircularTypedef  = errors.New("circular typedef")
    ErrCircularUses     = errors.New("circular dependency in uses")
    ErrAugmentNotFound  = errors.New("augment target not found")
    ErrDuplicateIdent   = errors.New("duplicate identifier")
    ErrListMissingKey   = errors.New("list key error")
    ErrConfigBoundary   = errors.New("config boundary violation")
    ErrInvalidDefault   = errors.New("invalid default value")
    ErrMandatoryDefault = errors.New("mandatory leaf with default")
    ErrTypeRestriction  = errors.New("invalid type restriction")
    ErrIdentityrefBase  = errors.New("invalid identityref base")
    ErrXPathSyntax      = errors.New("xpath syntax error")
    ErrMaxErrors        = errors.New("compilation stopped: too many errors")
)
```

Complete 21-site migration map (from RESEARCH.md):

| Approx Line | Current String Checked | Target Sentinel |
|-------------|------------------------|-----------------|
| 210  | "list invalid-list must have at least one key" | ErrListMissingKey |
| 240  | "duplicate identifier 'id'" | ErrDuplicateIdent |
| 269  | "has 'config true' but its parent base has 'config false'" | ErrConfigBoundary |
| 347  | "type string for node invalid-string cannot have range" | ErrTypeRestriction |
| 348  | "type int32 for node invalid-int cannot have length" | ErrTypeRestriction |
| 349  | "type int32 for node invalid-int cannot have pattern" | ErrTypeRestriction |
| 380  | "key 'not-here' not found in list 'bad-list-nonexistent'" | ErrListMissingKey |
| 381  | "key 'wrong-type' in list 'bad-list-wrong-type' must be a leaf" | ErrListMissingKey |
| 414  | "circular dependency detected in uses" | ErrCircularUses |
| 441  | "leaf 'invalid-leaf' is mandatory and cannot have a default value" | ErrMandatoryDefault |
| 939  | tt.errorMsg (table-driven, 3 cases) | ErrIdentityrefBase |
| 1139 | "invalid default 'True' for boolean leaf bad-bool" | ErrInvalidDefault |
| 1140 | "invalid default '12.5' for integer leaf bad-int" | ErrInvalidDefault |
| 1141 | "invalid default '-5' for unsigned integer leaf bad-uint" | ErrInvalidDefault |
| 1142 | "invalid default 'green' for enum leaf bad-enum" | ErrInvalidDefault |
| 1179 | "mismatched quotes in when expression" | ErrXPathSyntax |
| 1180 | "mismatched brackets or parentheses in must expression" | ErrXPathSyntax |
| 1181 | "mismatched brackets or parentheses in path expression" | ErrXPathSyntax |
| 1227 | "circular typedef" | ErrCircularTypedef |
| 1242 | "augment target not found" | ErrAugmentNotFound |
| 1324 | "compilation stopped" | ErrMaxErrors |

Note: Lines 347/348/349 use the same ErrTypeRestriction sentinel — one assert.ErrorIs is definitive; retain all three calls for documentation value (each call documents that 3 distinct type restriction errors are accumulated).
Note: Lines 380/381 use the same ErrListMissingKey sentinel — same rule.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 13 direct-path assert.Contains sites to assert.ErrorIs</name>
  <files>compiler/compiler_test.go</files>
  <action>
    Migrate the 13 call sites that route through c.addError() directly (no Validator involvement). These are the sites at approx lines 210, 240, 269, 347, 348, 349, 380, 381, 414, 441, 1227, 1242, 1324.

    For each site, replace:
      assert.Contains(t, err.Error(), "some substring")
    with:
      assert.ErrorIs(t, err, ErrXxx)

    Use the sentinel mapping table above. Do not change non-error assert.Contains calls (e.g., assert.Contains(t, schemaMod.Nodes, ...) or assert.Contains(t, cont.GetChildren(), ...)).

    For lines 347/348/349 (ErrTypeRestriction ×3): replace all three with three separate assert.ErrorIs(t, err, ErrTypeRestriction) calls. This documents that the compiler accumulates 3 distinct type restriction errors. Because errors.Is stops at first match, all three calls will pass — that is intentional. The repetition documents the expected accumulation count.

    For lines 380/381 (ErrListMissingKey ×2): same approach — two assert.ErrorIs(t, err, ErrListMissingKey) calls.

    Run the test suite to confirm these 13 sites pass before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /home/user/repos/gotya && go test ./compiler/... -count=1 -run "TestCompiler_ListMissingKey|TestCompiler_DuplicateIdent|TestCompiler_ConfigBoundary|TestCompiler_TypeRestriction|TestCompiler_CircularUses|TestCompiler_Mandatory|TestCompiler_Circular|TestCompiler_Augment|TestCompiler_MaxErrors" -v</automated>
  </verify>
  <done>
    The 13 direct-path sites use assert.ErrorIs. All targeted tests pass. No non-error assert.Contains calls were changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate 8 Validator-path assert.Contains sites and update identityref table test</name>
  <files>compiler/compiler_test.go</files>
  <action>
    Migrate the remaining 8 call sites (lines ~939, 1139-1142, 1179-1181) that route through the Validator. Plan 01 has already refactored the Validator to emit per-category sentinels, so these sites will now work correctly with assert.ErrorIs.

    **Table-driven test at line ~939 (TestCompiler_IdentityrefValidation or similar):**
    - Find the test struct definition with `errorMsg string` field
    - Rename the field to `sentinel error`
    - Update all 3 table case entries: replace the string message with `compiler.ErrIdentityrefBase`
      Example: `{name: "Missing Local Base", ..., sentinel: compiler.ErrIdentityrefBase}`
    - Change the assertion in the loop body from:
        `assert.Contains(t, err.Error(), tt.errorMsg)`
      to:
        `assert.ErrorIs(t, err, tt.sentinel)`

    **Lines ~1139-1142 (default value validation, 4 sites):**
    Replace each:
      `assert.Contains(t, err.Error(), "invalid default ...")`
    with:
      `assert.ErrorIs(t, err, ErrInvalidDefault)`

    **Lines ~1179-1181 (XPath syntax validation, 3 sites):**
    Replace each:
      `assert.Contains(t, err.Error(), "mismatched ...")`
    with:
      `assert.ErrorIs(t, err, ErrXPathSyntax)`

    After all migrations, verify zero assert.Contains(t, err.Error() calls remain:
      grep -n 'assert\.Contains(t, err\.Error()' compiler/compiler_test.go
    This must return no output.
  </action>
  <verify>
    <automated>cd /home/user/repos/gotya && go test ./compiler/... -count=1 -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - grep -n 'assert\.Contains(t, err\.Error()' compiler/compiler_test.go returns no results
    - Table-driven identityref test struct uses `sentinel error` field with compiler.ErrIdentityrefBase values
    - go test ./compiler/... passes with zero failures
    - go test ./... passes (full suite green)
  </done>
</task>

</tasks>

<verification>
grep -rn 'assert\.Contains(t, err\.Error()' /home/user/repos/gotya/compiler/compiler_test.go returns no output.
go test ./compiler/... -count=1 passes.
go test ./... -count=1 passes (full suite including gotya_test.go).
</verification>

<success_criteria>
- Zero assert.Contains(t, err.Error(), ...) calls remain in compiler/compiler_test.go
- All 21 former string-match sites use assert.ErrorIs(t, err, compiler.ErrXxx)
- Identityref table test struct field renamed from errorMsg string to sentinel error
- go test ./... passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-v1-gap-closure/06-03-SUMMARY.md`
</output>
