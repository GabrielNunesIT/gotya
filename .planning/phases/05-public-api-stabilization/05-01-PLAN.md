---
phase: 05-public-api-stabilization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - gotya_test.go
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04

must_haves:
  truths:
    - "All API-01/02/03 test functions exist in gotya_test.go and fail RED (compilation or t.Fatal)"
    - "gotya_test.go imports only the gotya package — no ast, schema, compiler imports"
    - "Test stubs cover: ParseError type assertion, multiple diagnostics, opaque ASTModule, opaque Module return from Compile"
  artifacts:
    - path: "gotya_test.go"
      provides: "Black-box test stubs for all Phase 5 API behaviors"
      exports: []
  key_links:
    - from: "gotya_test.go"
      to: "gotya package"
      via: "package gotya_test import"
      pattern: "package gotya_test"
---

<objective>
Write failing test stubs for all Phase 5 public API behaviors.

Purpose: Establish the RED baseline that Phase 5 plans 02 and 03 will turn GREEN. Having all test stubs in place before implementation ensures the implementation is driven by the observable contract, not inferred from the code.
Output: gotya_test.go with 6 test functions (package gotya_test), all failing.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-public-api-stabilization/05-CONTEXT.md
@.planning/phases/05-public-api-stabilization/05-RESEARCH.md
</context>

<feature>
  <name>Public API test stubs</name>
  <files>gotya_test.go</files>
  <behavior>
    - TestParse_Error: Call Parse("not valid yang") — assert errors.As(err, &parseErr) succeeds where parseErr is *gotya.ParseError. Currently fails: ParseError type does not exist.
    - TestParse_ErrorsAs: Same as above, also assert len(parseErr.Errors) > 0 and parseErr.Errors[0].Line > 0. Currently fails: type and fields don't exist.
    - TestParse_MultipleErrors: Call Parse with YANG that has two distinct syntax errors — assert len(parseErr.Errors) >= 2. Currently fails: multiple errors not plumbed.
    - TestCompile_OpaqueReturn: Call Parse(validYANG), then Compile([]*gotya.ASTModule{mod}, nil) — assert returned slice element type has a Schema() method accessible at compile time. Currently fails: Compile returns []*schema.Module which requires schema import; after fix Compile returns []*gotya.Module.
    - TestASTModule_Name: Call Parse(validYANG) — assert result.Name() returns non-empty string. Currently fails: ASTModule is an alias and Name() may not exist or alias exposes too many methods (test confirms encapsulation by ONLY calling Name()).
    - TestParseFile_FilledDiagnostic: Call ParseFile("nonexistent.yang") — error should be a file I/O error, not a ParseError. Call ParseFile with a real invalid YANG temp file — assert errors.As(err, &parseErr) and parseErr.Errors[0].File != "". Currently fails: ParseError type doesn't exist.
  </behavior>
  <implementation>
    Write test stubs using t.Fatal("not yet implemented") as stub body only for tests that cannot compile at all (type doesn't exist yet). For tests that compile but fail at runtime, write the assertion correctly so it fails RED when run. Use testify/require for assertions. The file must use package gotya_test (external test package). Import only "github.com/gotya/gotya" and stdlib (errors, testing, os). Do NOT import ast, schema, or compiler packages — that would defeat the purpose of API-03 testing.

    For the valid YANG source string used in TestCompile_OpaqueReturn and TestASTModule_Name, use a minimal but syntactically valid module:
    ```
    module test {
      yang-version 1;
      namespace "urn:test";
      prefix "t";
    }
    ```

    For TestParse_MultipleErrors, use a string that will produce multiple parser errors — e.g., a module body with two badly-formed statements inside braces.

    For TestParseFile_FilledDiagnostic, create a temp file with invalid YANG content using os.WriteFile to a t.TempDir() path, then call gotya.ParseFile() on it.

    After writing the stubs: run `go test . -count=1` — it must fail to compile (type ParseError not found) confirming RED state. That compilation failure IS the RED signal.
  </implementation>
</feature>

<verification>
  <automated>go test . -count=1 2>&1 | grep -E "ParseError|undefined|FAIL|ok" | head -20</automated>
</verification>

<success_criteria>
- gotya_test.go exists with 6 test functions
- File uses package gotya_test
- File imports only gotya and stdlib (no ast/schema/compiler)
- Running `go test .` fails with "undefined: gotya.ParseError" or similar — this is the expected RED state
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-api-stabilization/05-01-SUMMARY.md`
</output>
