---
phase: 03-go-generator
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - generator/golang/generator_anydata_test.go
  - generator/golang/generator_identityref_test.go
  - generator/golang/generator_rpc_test.go
  - generator/golang/generator_deviation_test.go
autonomous: true
requirements:
  - GOGEN-01
  - GOGEN-02
  - GOGEN-03
  - GOGEN-04

must_haves:
  truths:
    - "go test ./generator/golang/... -run 'TestAnyData|TestAnyXML|TestIdentityref|TestRPC|TestAction|TestNotification|TestDeviation' fails (RED) before any implementation"
    - "Each test file compiles cleanly — t.Fatal('not yet implemented') is the only body"
    - "Test names match the VALIDATION.md sampling map exactly so later plans can target them precisely"
  artifacts:
    - path: "generator/golang/generator_anydata_test.go"
      provides: "Failing stubs: TestAnyData, TestAnyXML"
      contains: "not yet implemented"
    - path: "generator/golang/generator_identityref_test.go"
      provides: "Failing stubs: TestIdentityref, TestIdentityrefCrossModule"
      contains: "not yet implemented"
    - path: "generator/golang/generator_rpc_test.go"
      provides: "Failing stubs: TestRPC, TestAction, TestNotification"
      contains: "not yet implemented"
    - path: "generator/golang/generator_deviation_test.go"
      provides: "Failing stubs: TestDeviationNotSupported, TestDeviationReplace, TestDeviationAddDelete"
      contains: "not yet implemented"
  key_links:
    - from: "generator_anydata_test.go"
      to: "golang.New().GenerateDevice()"
      via: "compile helper → GenerateDevice → assert output contains json.RawMessage"
      pattern: "json\\.RawMessage"
    - from: "generator_identityref_test.go"
      to: "golang.New().GenerateDevice()"
      via: "compile helper → GenerateDevice → assert no *string for identityref leaf"
      pattern: "Identity"
    - from: "generator_rpc_test.go"
      to: "golang.New().GenerateDevice() or Generate()"
      via: "compile helper → assert XxxInput/XxxOutput structs present"
      pattern: "Input|Output"
    - from: "generator_deviation_test.go"
      to: "golang.New().GenerateDevice()"
      via: "compile helper → assert deviated node absent or type changed"
      pattern: "deviate"
---

<objective>
Write failing test stubs (RED phase) for all four Go generator gaps. Each stub compiles,
fails immediately via t.Fatal("not yet implemented"), and targets the exact function name
referenced in VALIDATION.md so subsequent plans can run a precise -run filter.

Purpose: Nyquist compliance — every implementation task in plans 02-05 must have a pre-existing
test that turns GREEN. Stubs are the contract; implementation fills them.

Output:
- 4 new test files under generator/golang/
- 9 failing tests total (2 + 2 + 3 + 3 = 10, minus TestNotification which is GOGEN-03 but
  listed under generator_rpc_test.go)
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-go-generator/03-RESEARCH.md
@.planning/phases/03-go-generator/03-VALIDATION.md

<interfaces>
<!-- Existing test helper pattern — all generator tests follow this structure -->
<!-- From generator/golang/generator_test.go -->
```go
package golang_test

import (
    "bytes"
    "go/format"
    "strings"
    "testing"

    "github.com/gotya/gotya/compiler"
    "github.com/gotya/gotya/generator/golang"
    "github.com/gotya/gotya/parser"
    "github.com/gotya/gotya/parser/lexer"
    "github.com/stretchr/testify/assert"
    "github.com/stretchr/testify/require"
)

// Helper pattern: parse → compile → GenerateDevice → check output
func compileYANG(t *testing.T, src string) *schema.Module { ... }
```

<!-- Key generator API used by tests -->
```go
// generator/golang/generator.go
func New(opts *Options) generator.Generator
func (g *GoGenerator) GenerateDevice(modules []*schema.Module, w io.Writer) error
func (g *GoGenerator) Generate(mod *schema.Module, w io.Writer) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing stubs for GOGEN-02 and GOGEN-01</name>
  <files>generator/golang/generator_anydata_test.go, generator/golang/generator_identityref_test.go</files>
  <behavior>
    - TestAnyData: compile a YANG module with an `anydata` node, call GenerateDevice, FAIL because output currently lacks json.RawMessage
    - TestAnyXML: same but with `anyxml` node
    - TestIdentityref: compile a YANG module with an identity + identityref leaf, call GenerateDevice, FAIL because output currently has *string not a typed const
    - TestIdentityrefCrossModule: same but identity base is in an imported module — FAIL for same reason
  </behavior>
  <action>
    Create generator/golang/generator_anydata_test.go (package golang_test):

    TestAnyData — inline YANG with `anydata state;` inside a container, call
    golang.New(&golang.Options{PackageName:"testpkg"}).GenerateDevice([]*schema.Module{mod}, &buf),
    then t.Fatal("not yet implemented").

    TestAnyXML — same but `anyxml state;`.

    Create generator/golang/generator_identityref_test.go (package golang_test):

    TestIdentityref — inline YANG defining:
      identity base-identity;
      identity derived-one { base base-identity; }
      leaf my-leaf { type identityref { base base-identity; } }
    Call GenerateDevice, then t.Fatal("not yet implemented").

    TestIdentityrefCrossModule — use TWO modules: one defines the identity, the second imports
    it and has a leaf with identityref base pointing to the first. Both modules passed to
    GenerateDevice. Then t.Fatal("not yet implemented").

    Use the same parse/compile helper pattern as existing generator_test.go:
      lexer.New → parser.New → p.ParseModule() → compiler.New(nil).Compile() → require.NoError.
    Do NOT add a shared helper function — each test is self-contained.

    Import list: bytes, strings, testing, compiler, golang, parser, lexer, schema, assert, require.
    (strings and assert may be unused at stub stage — add _ import if needed to compile, or
    simply omit and let the t.Fatal stub be the only body — no other assertions).
  </action>
  <verify>
    <automated>cd /home/user/repos/gotya && go test ./generator/golang/... -run 'TestAnyData|TestAnyXML|TestIdentityref|TestIdentityrefCrossModule' -count=1 2>&1 | grep -E 'FAIL|not yet implemented'</automated>
  </verify>
  <done>Both files compile. All 4 tests fail with "not yet implemented". No compile errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing stubs for GOGEN-03 and GOGEN-04</name>
  <files>generator/golang/generator_rpc_test.go, generator/golang/generator_deviation_test.go</files>
  <behavior>
    - TestRPC: YANG module with an rpc statement (input + output children), GenerateDevice, FAIL
    - TestAction: YANG module with a container that has an action sub-statement, GenerateDevice, FAIL
    - TestNotification: YANG module with notification statement (flat data children), GenerateDevice, FAIL
    - TestDeviationNotSupported: two modules — target module with a leaf, deviation module removes it — FAIL
    - TestDeviationReplace: deviation module replaces a leaf type — FAIL
    - TestDeviationAddDelete: deviation adds/removes a property — FAIL
  </behavior>
  <action>
    Create generator/golang/generator_rpc_test.go (package golang_test):

    TestRPC — inline YANG:
      rpc reset-counters {
        input { leaf target { type string; } }
        output { leaf status { type string; } }
      }
    Call GenerateDevice, then t.Fatal("not yet implemented").

    TestAction — inline YANG with a container that has:
      action ping {
        input { leaf host { type string; } }
        output { leaf result { type string; } }
      }
    Call GenerateDevice, then t.Fatal("not yet implemented").

    TestNotification — inline YANG:
      notification link-up {
        leaf interface-name { type string; }
      }
    Call GenerateDevice, then t.Fatal("not yet implemented").

    Create generator/golang/generator_deviation_test.go (package golang_test):

    TestDeviationNotSupported — compile a module with leaf `mtu { type uint16; }`, then apply
    a deviation module that says `deviation /base:mtu { deviate not-supported; }`. Compile both
    and call GenerateDevice. Then t.Fatal("not yet implemented").

    Note: the deviation test is complex to set up inline because the compiler must process
    the deviation. Keep the YANG simple — a single-container module with one leaf, and a
    deviation module that removes it. Use two separately compiled modules passed together to
    GenerateDevice. If the compiler does not yet support merging deviation modules at compile
    time, use a pre-compiled schema.Module with a manually constructed Deviation object in
    the test (struct literal) — this is acceptable for stub stage.

    TestDeviationReplace — same setup but deviate replace type to string. t.Fatal("not yet implemented").

    TestDeviationAddDelete — deviate add a min-elements constraint. t.Fatal("not yet implemented").

    Do NOT share helpers between test files. Each test is self-contained.
  </action>
  <verify>
    <automated>cd /home/user/repos/gotya && go test ./generator/golang/... -run 'TestRPC|TestAction|TestNotification|TestDeviation' -count=1 2>&1 | grep -E 'FAIL|not yet implemented'</automated>
  </verify>
  <done>Both files compile. All 6 tests fail with "not yet implemented". go test ./generator/golang/... -count=1 (without filter) still passes all pre-existing tests.</done>
</task>

</tasks>

<verification>
Run the full suite to confirm stubs do not break existing tests:
  cd /home/user/repos/gotya && go test ./... -count=1

Confirm stub RED state:
  go test ./generator/golang/... -run 'TestAnyData|TestAnyXML|TestIdentityref|TestRPC|TestAction|TestNotification|TestDeviation' -count=1

Expected: 10 FAIL entries, 0 PASS entries for the new tests.
Existing TestGoGenerator_* tests must still PASS.
</verification>

<success_criteria>
- 4 new test files exist under generator/golang/
- 10 failing tests total (all t.Fatal("not yet implemented"))
- All existing generator tests still pass
- go build ./... succeeds (test files compile)
</success_criteria>

<output>
After completion, create `.planning/phases/03-go-generator/03-01-SUMMARY.md`
</output>
